{
  "private": true,
  "version": "1.1.1",
  "devDependencies": {},
  "dependencies": {
    "bootstrap": "^3.3.6",
    "jquery": "^3.0.0",
    "markdown": "^0.5.0"
  },
  "scripts": {
    "version": "git add package.json",
    "postversion": "git push origin && git push --tags"
  }
}
